# Reference for all configurable environment variables.
# Prisma's DATABASE_URL must go in packages/chat/.env (see packages/chat/.env.example).
# The rest are optional shell env vars with sensible defaults.

# Hono server port
PORT=8787

# Claude model to use for chat
LLM_MODEL=claude-opus-4-6

# Path to the Willow knowledge graph JSON file
WILLOW_GRAPH_PATH=~/.willow/graph.json
